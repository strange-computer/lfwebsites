---
import MainLayout from '../layouts/MainLayout.astro';
---
<MainLayout title="Contact | Lake Forest Websites" bodyClass="page-contact">
  <link rel="stylesheet" href="/css/pages/contact.css" />

  <!-- Hero -->
  <header class="relative pt-32 md:pt-40 pb-12 md:pb-16 px-4 md:px-6 text-center">
    <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[800px] h-[600px] glow-bg pointer-events-none opacity-40"></div>
    <div class="max-w-5xl mx-auto relative">
      <div class="hero-content inline-flex items-center gap-2 px-3 py-1 rounded-full bg-white/5 border border-white/10 text-xs font-medium text-orange-400 mb-6 backdrop-blur-sm">
        <span class="w-2 h-2 rounded-full bg-orange-500 animate-pulse"></span>
        Contact
      </div>
      <h1 class="hero-content text-3xl md:text-6xl font-bold tracking-tight mb-4 text-balance" style="animation-delay: 0.1s">
        Talk to Our <span class="text-gradient-orange">Team</span>
      </h1>
      <p class="hero-content text-zinc-400 text-base md:text-lg leading-relaxed px-2 md:px-0" style="animation-delay: 0.2s">
        Have a question or want to discuss a project? Send a note and a local team member will respond promptly during business hours.
      </p>
    </div>
  </header>

  <main class="pb-16 md:pb-24 px-4 md:px-6">
    <section class="max-w-7xl mx-auto grid md:grid-cols-3 gap-4 md:gap-6 mb-8 md:mb-12 border-t border-white/5 pt-8 md:pt-12">
      <div class="framer-card p-8 rounded-2xl fade-in-up stagger-1">
        <h3 class="text-xl font-semibold mb-2">Call Us</h3>
        <p class="text-zinc-400">Mon–Fri, 8am–5pm</p>
        <a href="tel:(555) 555-1234" class="mt-3 inline-block text-white font-semibold hover:text-zinc-300 min-h-[44px] flex items-center">(555) 555-1234</a>
      </div>
      <div class="framer-card p-8 rounded-2xl fade-in-up stagger-2">
        <h3 class="text-xl font-semibold mb-2">Email</h3>
        <p class="text-zinc-400">We reply quickly during business hours.</p>
        <a href="mailto:info@example.com" class="mt-3 inline-block text-white font-semibold hover:text-zinc-300 min-h-[44px] flex items-center">info@example.com</a>
      </div>
      <div class="framer-card p-8 rounded-2xl fade-in-up stagger-3">
        <h3 class="text-xl font-semibold mb-2">Location</h3>
        <p class="text-zinc-400 leading-relaxed">Lake Forest, CA</p>
      </div>
    </section>

    <!-- Contact Form -->
    <section class="max-w-4xl mx-auto border-t border-white/5 pt-8 md:pt-12 mesh-gradient py-8 md:py-12">
      <div class="framer-card p-8 rounded-2xl fade-in-up">
        <h2 class="text-2xl font-bold mb-6 text-balance">How can we help you today?</h2>
        <form id="lfwContactForm" class="grid md:grid-cols-2 gap-4">
          <div class="col-span-2 md:col-span-1">
            <label class="block text-sm text-zinc-400 mb-1">Full Name*</label>
            <input name="name" required class="w-full rounded-xl bg-white/5 border border-white/10 px-4 py-3 text-white placeholder-zinc-500 focus:outline-none focus:ring-2 focus:ring-orange-600 min-h-[44px]" placeholder="Jane Doe" />
          </div>
          <div class="col-span-2 md:col-span-1">
            <label class="block text-sm text-zinc-400 mb-2">Email*</label>
            <input type="email" name="email" required class="w-full rounded-xl bg-white/5 border border-white/10 px-4 py-3 text-white placeholder-zinc-500 focus:outline-none focus:ring-2 focus:ring-orange-600 min-h-[44px]" placeholder="you@example.com" />
          </div>
          <div class="col-span-2 md:col-span-1">
            <label class="block text-sm text-zinc-400 mb-2">Phone</label>
            <input name="phone" class="w-full rounded-xl bg-white/5 border border-white/10 px-4 py-3 text-white placeholder-zinc-500 focus:outline-none focus:ring-2 focus:ring-orange-600 min-h-[44px]" placeholder="(555) 123-4567" />
          </div>
          <div class="col-span-2">
            <label class="block text-sm text-zinc-400 mb-2">Your Message</label>
            <textarea name="message" rows="5" class="w-full rounded-xl bg-white/5 border border-white/10 px-4 py-3 text-white placeholder-zinc-500 focus:outline-none focus:ring-2 focus:ring-orange-600 min-h-[120px]" placeholder="Tell us a bit about your project..."></textarea>
          </div>
          <div class="col-span-2 flex items-center gap-3">
            <button type="submit" class="px-8 py-3 bg-orange-600 text-white font-semibold rounded-full hover:bg-orange-500 transition-colors btn-glow">
              Send Message
            </button>
            <span id="lfwFormNote" class="text-sm text-zinc-500">No spam. We’ll reply during business hours.</span>
          </div>
        </form>
      </div>
    </section>

    <!-- Map -->
    <section class="max-w-7xl mx-auto mt-8 md:mt-12 border-t border-white/5 pt-8 md:pt-12">
      <div class="framer-card p-6 rounded-2xl fade-in-up">
        <div class="flex items-center justify-between mb-4">
          <h2 class="text-2xl font-bold text-balance">Lake Forest, CA</h2>
          <a href="https://www.google.com/maps?q=Lake+Forest,+CA" target="_blank" rel="noreferrer" class="text-sm text-zinc-400 hover:text-white transition-colors">
            Open in Google Maps
          </a>
        </div>
        <div class="w-full h-80 md:h-96 rounded-xl overflow-hidden border border-white/10">
          <iframe
            title="Map of Lake Forest, CA"
            class="w-full h-full"
            loading="lazy"
            referrerpolicy="no-referrer-when-downgrade"
            src="https://www.google.com/maps?q=Lake%20Forest,%20CA&z=12&output=embed">
          </iframe>
        </div>
      </div>
    </section>
  </main>

  <script>
    // Simple mailto submit (no backend)
    (function () {
      const form = document.getElementById('lfwContactForm');
      if (!form) return;
      form.addEventListener('submit', (e) => {
        e.preventDefault();
        const fd = new FormData(form);
        const name = encodeURIComponent(fd.get('name') || '');
        const email = encodeURIComponent(fd.get('email') || '');
        const phone = encodeURIComponent(fd.get('phone') || '');
        const msg = encodeURIComponent(fd.get('message') || '');
        const subject = encodeURIComponent('New inquiry from website');
        const body = encodeURIComponent(
          `Name: ${decodeURIComponent(name)}\nEmail: ${decodeURIComponent(email)}\nPhone: ${decodeURIComponent(phone)}\n\nMessage:\n${decodeURIComponent(msg)}`
        );
        window.location.href = `mailto:info@example.com?subject=${subject}&body=${body}`;
      });
    })();
  </script>
</MainLayout>


